# -*- coding: utf-8 -*-
# Single, minimal requirements for the core pipeline (STT + alignment + SER + SRT/ASS)

# PyTorch (install CUDA-enabled wheel matching your system if you want GPU)
torch==2.2.0
torchaudio==2.2.0

# WhisperX ASR + alignment
whisperx==3.2.0
faster-whisper==1.0.0
ctranslate2==4.4.0

# Audio/ML utilities
librosa==0.10.1
numpy==1.26.4
transformers==4.36.2
tokenizers
huggingface-hub>=0.19.3
tqdm
pytorch-lightning==2.1.3
torchmetrics==1.2.1

# Media I/O and helpers
soundfile>=0.12.0
ffmpeg-python==0.2.0
pydub>=0.25.0
opencv-python==4.8.1.78

# Optional: diarization (pyannote.audio) is NOT installed by default.
# To enable diarization, install additionally:
#   pip install "pyannote.audio==3.1.1"
